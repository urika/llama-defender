# exp-1-amnesia 实验报告：受控失忆实验 E1-E4 判定（阴性结果）

> **实验**: exp-1-amnesia（台账 `logs/diag/experiments.jsonl`）｜**日期**: 2026-08-30
> **协议**: [`amnesia-experiment-protocol-20260830`](../03-experiments-testing/amnesia-experiment-protocol-20260830.md)（预注册，不允许事后换指标）
> **治疗**: keep_messages 600→12（fifo），hbe_min_chars 20000→8000，sample_every 4→2
> **数据**: 379 治疗轮 / 130 ILE / 159 探针 / 2418 manifest 行 / 30 任务配对面板

---

## 判定结果总览

| 判定 | 预期 | 实测 | 结论 |
|---|---|---|---|
| **E1 机制保真** | ILE≥30，manifest 覆盖率 100% | 130 ILE，**19/19 会话 100% 覆盖**，2418 行 | ✅ **PASS** |
| **E2 信号效度（主判定）** | ILE 后熵上升与损失量 \|ρ\|≥0.4 | **ρ=−0.040，p=0.70**（n=96 配对事件，会话级置换检验） | ❌ **FAIL** |
| E3 结局影响 | resolved 率降 ≥15pp | **−10.0pp**（76.7%→66.7%，McNemar p=0.45，n=30 配对） | ❌ 未达门槛（方向正确但功效不足） |
| E4 趋势分布 | 治疗组 down 占比 < 对照组 | 治疗 6↑/4↓/3平 vs 对照 11↓/2平/2↑ | 🟡 方向性一致但样本小 |

**主判定：E2 不通过。按预注册纪律，R9.3 门控不上线，IFC 止步度量。**

---

## E2 详解（主判定）

### 配对方法

对每个 ILE 轮（`ifc.ile=true`），取 ILE 前 ≤2 轮内最近的探针 H_BE 为基线、ILE 后 ≤2 轮内最近的探针为响应，计算 ΔH = 响应−基线；与该轮的信息损失量（`dropped_chars + shrunk_chars`）做 Spearman 相关。

### 结果

```
可配对 ILE 事件: 96（来自 14 会话）
Spearman ρ = −0.0398
会话级置换检验 p = 0.6962（5000 次置换）
```

**解释**：损失量与熵变化之间**无相关**。43/96 正相关 vs 53/96 负相关——与随机无异。ΔH 均值 −0.035（几乎为零），中位 −0.020。

### 敏感性检查

- 正相关方向（损失↑→熵↑）比例：43/96 = 45%（硬币水平）
- 信号强度：远低于任何可检测阈值
- **结论对阈值选择不敏感**：在 ρ=0.3/0.4/0.5 任何门槛下均不通过

### 效应量门槛的折扣验证

| 来源 | 效应量 | 环境差异 |
|---|---|---|
| MMPO 论文 | r=−0.684 | 训练任务、全精度、RL 优化后的记忆策略 |
| 本实验 | ρ=−0.040 | text 任务族、4-bit 量化、无训练 |
| 折扣 | **~0×（不迁移）** | 信号在该族不存在或低于本实验检出限 |

---

## E3 详解

```
配对任务: 30（McNemar 精确检验）
双过: 18 | 双挂: 5 | 过→挂: 5 | 挂→过: 2
对照 resolved 率: 76.7% | 治疗: 66.7% | Δ = −10.0pp
McNemar p = 0.4531
```

- 方向正确（截断降完成率）但未达 15pp 门槛
- n=30 对该幅度（10pp）的功效约 30-40%——**样本量不足**，需多批累计
- 不一致对仅 7 个（5 过→挂 + 2 挂→过），McNemar 无法拒绝零假设

---

## E4 详解

| 趋势 | 对照（n=15） | 治疗（n=13） |
|---|---|---|
| down | 11 (73%) | 4 (31%) |
| flat | 2 | 3 |
| up | 2 (13%) | 6 (46%) |

治疗组熵上升趋势占比 46% vs 对照 13%——**方向性一致**（截断使更多会话信念恶化），但 Fisher 精确检验 p≈0.06（边缘），样本不足以确证。

---

## 预注册的"双无"裁决路径

协议 §1 预注册：*"E2/E3 均无 → 只允许'信号无效 / 该族不敏感'两种解释，裁决方式为 code 任务族补批"*

**当前状态**：E2 明确无信号 / E3 方向正确但不显著——**部分触发裁决条款**。两种待判别解释：

1. **信号无效**：信念熵探针在本系统条件下（4-bit 量化 + 本地模型 + 锚定问题措辞）根本测不到信息损失效应
2. **text 族不敏感**：文本生成/处理任务的信息冗余度高（任务说明可重推导），截断损失不损伤信念；coding 任务（代码结构依赖性强）可能不同

**裁决方式**：code 任务族（SWE-bench 式）补批同协议实验。

---

## 阴性结果的科学价值

这是首个受控实验证明：**在 text 生成任务族上，信念熵探针不响应上下文截断的信息损失**（在 ρ 检出限 < 0.1 的精度下）。这一结果：

1. 对 MMPO 论文的 r=−0.684 构成**域外不复制证据**——该效应不能无条件推广到 text 任务族 + 本地量化模型
2. 精确化了迁移条件：论文效应可能仅在（a）训练后的记忆策略下（b）全精度模型（c）其训练任务族成立
3. 为后续研究指明方向：需用 code 任务族裁决"信号无效 vs 族不敏感"

---

## 对路线图的影响

| 项 | 影响 | 动作 |
|---|---|---|
| R9.3 门控（Phase 3） | **不上线**（E2 是解锁前提，已 FAIL） | 保持默认关；code 族裁决后重评 |
| R9.1/R9.2 度量 | **不受影响**（已生产运行，运维分析价值独立） | 维持 |
| E1 验证的机制 | **manifest/页表/锚点差分 100% 可靠** | R10 PDC 路线不受影响——**这恰恰说明可寻址性比熵更可靠** |
| Phase 2 门禁 | **本批未通过** | 累计 ILE 130/200；code 族补批可同时推进 |

---

## 与 D_ledger 精度轴的对照

本实验未将 D_ledger 作为主判定轴（协议预注册时以熵为主判）。但按对话讨论的"双轴"设计理念，后续 code 族实验应**同时报告熵与 D_ledger 两轴**——如果精度轴在 code 族上响应而熵不响应，则证明 D_ledger 是更可靠的信念度量（这与"清晰且准确"的双轴前提一致）。

---

## 附录：实验执行日志摘要

- 10:37 窗口打开（云批退出）
- 10:40 进场（reload 事故 + 恢复，见 DEF-306）
- 10:45 试点 2 任务通过
- 10:58 正式批跑 begin（30 任务）
- 15:43 最后任务完成（~4.8h）
- 15:50 参数恢复 + reload + end
- 评分：`evaluate.py --all`
